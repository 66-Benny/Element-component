import ScmFixedWorkOrderMenu from './src/fixed-work-order-menu.vue';

/* istanbul ignore next */
ScmFixedWorkOrderMenu.install = (Vue) => {
  Vue.component(ScmFixedWorkOrderMenu.name, ScmFixedWorkOrderMenu);
};

export default ScmFixedWorkOrderMenu;
