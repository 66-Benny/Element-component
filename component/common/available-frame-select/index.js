import AvailableFrameSelect from './src/available-frame-select';

/* istanbul ignore next */
AvailableFrameSelect.install = (Vue) => {
  Vue.component(AvailableFrameSelect.name, AvailableFrameSelect);
};

export default AvailableFrameSelect;
