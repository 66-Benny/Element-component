import ScmEmployeeSelect from './src/employee-select';

/* istanbul ignore next */
ScmEmployeeSelect.install = (Vue) => {
  Vue.component(ScmEmployeeSelect.name, ScmEmployeeSelect);
};

export default ScmEmployeeSelect;
