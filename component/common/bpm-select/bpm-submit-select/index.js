import ScmBpmSubmitSelect from './src/bpm-submit-select';

/* istanbul ignore next */
ScmBpmSubmitSelect.install = (Vue) => {
  Vue.component(ScmBpmSubmitSelect.name, ScmBpmSubmitSelect);
};

export default ScmBpmSubmitSelect;
