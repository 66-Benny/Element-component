import ScmBpmRejectCard from './src/bpm-reject';

/* istanbul ignore next */
ScmBpmRejectCard.install = (Vue) => {
  Vue.component(ScmBpmRejectCard.name, ScmBpmRejectCard);
};

export default ScmBpmRejectCard;
