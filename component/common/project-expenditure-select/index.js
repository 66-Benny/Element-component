import ProjectExpenditureSelect from './src/project-expenditure-select';

/* istanbul ignore next */
ProjectExpenditureSelect.install = (Vue) => {
  Vue.component(ProjectExpenditureSelect.name, ProjectExpenditureSelect);
};

export default ProjectExpenditureSelect;
